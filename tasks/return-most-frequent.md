Task: given a word, return the file which contains the most incidents of that word

(Similarly, and I don't know if this would be harder or easier, given a file, return the most common word in that file.)
